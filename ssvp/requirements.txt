# Core deep learning
torch>=2.1.0
torchvision>=0.16.0

# CLIP (OpenAI)
# Install via: pip install git+https://github.com/openai/CLIP.git
# or: pip install openai-clip
openai-clip>=1.0.1

# DINOv2 (loaded via torch.hub — no separate package needed,
# but timm is required by torch.hub's DINOv2 implementation)
timm>=0.9.0

# Image processing
Pillow>=10.0.0
matplotlib>=3.7.0
numpy>=1.24.0
scipy>=1.11.0
